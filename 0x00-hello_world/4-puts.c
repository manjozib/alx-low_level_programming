#include<stdio.h>
#include<string.h>

int main(void)
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
{
char myString[] = "\"Programming is like building a multilingual puzzle";
puts(myString);
return (0);
}
